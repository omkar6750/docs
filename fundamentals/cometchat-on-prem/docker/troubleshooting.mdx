---
title: "Troubleshooting"
sidebarTitle: "Troubleshooting"
---

Common operational issues and debugging guidance.

## Common problems and likely causes

### 502 errors
- Possible cause: Chat API unreachable or unhealthy behind NGINX.
- Resolution:
  - Ensure the Chat API service is running: `docker service ps <chat-api-service>`
  - Check NGINX logs and upstream configuration to verify routing and upstream health.

### Kafka lag
- Possible cause: Consumer slowdown or insufficient partition count.
- Resolution:
  - Check Kafka consumer lag: `kafka-consumer-groups --describe --group <your-consumer-group> --bootstrap-server <kafka-broker>`
  - Increase partitions if needed: `kafka-topics --alter --partitions <new-partition-count> --topic <your-topic> --bootstrap-server <kafka-broker>`

### Redis eviction
- Possible cause: Memory pressure or incorrect eviction policy.
- Resolution:
  - Inspect memory settings: `redis-cli config get maxmemory` and `redis-cli config get maxmemory-policy`
  - Set an eviction policy such as `redis-cli config set maxmemory-policy allkeys-lru`

### TiKV region errors
- Possible cause: Disk latency, resource contention, or store imbalance.
- Resolution:
  - Check TiKV store status: `tiup cluster display`
  - Rebalance regions if needed: `tiup cluster restart <cluster-name> --force`

## Debugging commands

### Container and Swarm diagnostics
- View container logs: `docker logs <container-name>`
- Check service status: `docker service ps <service-name>`
- Inspect container details: `docker inspect <container-name>`

### TiDB cluster status
- Display cluster status with TiUP: `tiup cluster display <cluster-name>`

---

## What's next

Now that you understand troubleshooting procedures, explore advanced deployment scenarios:

**Next Step:**
- [Air-Gapped Deployment](./air-gapped-deployment) - Deploy in restricted network environments

**Related Documentation:**
- [Monitoring](./monitoring) - Set up comprehensive monitoring to detect issues early
- [Scaling](./scaling) - Troubleshoot performance issues related to scaling
- [Upgrades](./upgrades) - Troubleshoot upgrade and rollback issues
- [Security](./security) - Diagnose security-related issues and access problems

**Troubleshooting Best Practices:**
- Monitor system health proactively to detect issues before user impact
- Document common issues and resolutions in runbooks
- Maintain detailed logs for post-incident analysis
- Test troubleshooting procedures in staging environments
- Keep contact information for enterprise support readily available

**Need additional help?** For complex issues, performance optimization, or architectural guidance, contact **enterprise@cometchat.com** or submit a request via our [contact form](https://www.cometchat.com/contact). Enterprise support teams typically respond within 1 business day.
