---
title: Advanced JavaScript Controls
---

import { Callout, AccordionGroup, Accordion } from 'mintlify';
import ChatCallMethods from '/snippets/widget/chat-call-methods.mdx'
import UIEventListeners from '/snippets/widget/ui-event-listeners.mdx'
import StorageMethods from '/snippets/widget/storage-methods.mdx'
import UserGroupManagement from '/snippets/widget/user-group-management.mdx'
import AuthMethods from '/snippets/widget/auth-methods.mdx'
import LocalizationMethods from '/snippets/widget/localization.mdx'

## Advanced JavaScript Controls

The embed works out of the box. Use the helpers below only if you need to open a specific chat, listen for widget events, or change settings on the fly.

<Callout>
Skip this section if you only need the default widget behavior.
</Callout>

### Before you copy anything

1. Keep the standard widget snippet (from the Integration guide) on your page.  
2. Add another `<script type="module">` right after it or inside your site builder’s “custom code” area.  
3. Wrap your code in `window.addEventListener("DOMContentLoaded", () => { ... })` so it runs after the widget loads.  
4. Replace placeholder text such as `UID`, `GUID`, and `LANGUAGE_CODE` with your real values.

When the widget is ready, it exposes a global helper named `CometChatApp`. Every example below shows a tiny recipe you can paste inside the script mentioned above.

<AccordionGroup>
  <Accordion title="Open a chat or start a call">
    <p>Tell the widget exactly which user or group to open, or trigger a voice/video call in one click.</p>
    <ChatCallMethods />
    <p className="mt-3 text-sm text-gray-600 dark:text-gray-300">
      Replace `UID` with the person’s unique ID and `GUID` with the group ID you created in CometChat.
    </p>
  </Accordion>

  <Accordion title="Listen for widget events">
    <p>Run your own code when someone sends a message, opens the docked widget, or switches chats.</p>
    <UIEventListeners />
  </Accordion>

  <Accordion title="Create users or groups on the fly">
    <p>Add people or groups directly from the page without visiting the CometChat Dashboard.</p>
    <UserGroupManagement />
  </Accordion>

  <Accordion title="Log users in or out">
    <p>Control sessions manually—for example, log in with an auth token or listen for logout events.</p>
    <AuthMethods />
  </Accordion>

  <Accordion title="Control where data is stored">
    <p>Pick session storage (clears on tab close) or local storage (persists across visits) before launching the widget.</p>
    <StorageMethods />
  </Accordion>

  <Accordion title="Change the widget language">
    <p>Force the widget to a specific language code instead of relying on the browser’s default.</p>
    <LocalizationMethods />
  </Accordion>
</AccordionGroup>
