---
openapi: post /ai-agents/tools
description: "Defines custom tools that agents can invoke during conversations.  **Naming:** The tool name must be unique per application.  **Type:** When `doNotExecute` is `true`, the type is set to `tool`. Otherw"
---

For the complete error reference, see [Error Guide](/rest-api/byo-ai-agents-apis/error-codes).
