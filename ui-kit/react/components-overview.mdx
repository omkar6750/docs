---
title: "Overview"
description: "Browse all prebuilt UI components in the CometChat React UI Kit — conversations, messages, users, groups, calls, search, and AI."
---

<Accordion title="AI Agent Component Spec">
- **Package:** `@cometchat/chat-uikit-react`
- **Required setup:** `CometChatUIKit.init()` + `CometChatUIKit.login()` before rendering any component
- **Component API pattern:**
  - Callback actions: `on<Event>={(param) => {}}`
  - Data filtering: `<entity>RequestBuilder={new CometChat.<Entity>RequestBuilder()}`
  - Toggle features: `hide<Feature>={true|false}`
  - Custom rendering: `<slot>View={(<params>) => JSX}`
  - CSS overrides: Target `.cometchat-<component>` class in global CSS
- **Calling:** Requires separate `@cometchat/calls-sdk-javascript` package
</Accordion>

## Overview

CometChat's **UI Kit** is a set of pre-built UI components that allows you to easily craft an in-app chat with all the essential messaging features. All components are imported from the `@cometchat/chat-uikit-react` package and share a consistent API pattern for actions, filters, events, and styling.

## Why Use / When to Use

- Add a full-featured conversations list to display recent chats with the [Conversations](/ui-kit/react/conversations) component
- Render messages for a selected conversation using the [Message List](/ui-kit/react/message-list), [Message Composer](/ui-kit/react/message-composer), and [Message Header](/ui-kit/react/message-header) components
- Display and manage users and groups with the [Users](/ui-kit/react/users), [Groups](/ui-kit/react/groups), and [Group Members](/ui-kit/react/group-members) components
- Integrate voice and video calling with the [Call Buttons](/ui-kit/react/call-buttons), [Incoming Call](/ui-kit/react/incoming-call), [Outgoing Call](/ui-kit/react/outgoing-call), and [Call Logs](/ui-kit/react/call-logs) components
- Add search functionality across conversations, users, groups, and messages with the [Search](/ui-kit/react/search) component
- Enable AI-powered chat assistance with the [AI Assistant Chat](/ui-kit/react/ai-assistant-chat) component
- Customize message rendering and templates using the [Message Template](/ui-kit/react/message-template) system

## Usage

### Actions

Actions direct the operational behavior of a component. They are split into two categories: Predefined Actions and User-Defined Actions.

#### Predefined Actions

These are actions that are inherently programmed into a UI component. They are ingrained in the component itself by default, and they execute automatically in response to user interaction, without needing any additional user input.

#### User-Defined Actions

These are actions that must be explicitly specified by the user. They are not innately part of the component like predefined actions. Instead, they must be developed based on the unique needs of the user or the application. User-defined actions provide adaptability and allow for the creation of custom behaviors that align with the individual needs of the application.

To customize the behavior of a component, actions can be overridden by you. This provides you with control over how the component responds to specific events or interactions.

All components expose actions to the user, which means that users can interact with these types of components through predefined or user-defined actions.

### Events

Events allow for a decoupled, flexible architecture where different parts of the application can interact without having to directly reference each other. This makes it easier to create complex, interactive experiences, as well as to extend and customize the functionality provided by the CometChat UI Kit.

All Components have the ability to emit events. These events are dispatched in response to certain changes or user interactions within the component. By emitting events, these components allow other parts of the application to react to changes or interactions, thus enabling dynamic and interactive behavior within the application.

{/* ## Advanced Features

Advanced customization options such as custom view slots, theming, and global event handling are documented on each individual component page. See the component links in the [Quick Reference](#overview) above or visit specific pages:

- **Custom views and view slots:** [Conversations](/ui-kit/react/conversations), [Message List](/ui-kit/react/message-list), [Message Header](/ui-kit/react/message-header), [Incoming Call](/ui-kit/react/incoming-call)
- **Theming and CSS customization:** [Theming](/ui-kit/react/theme)
- **Global events:** [Events](/ui-kit/react/events)
- **Message templates:** [Message Template](/ui-kit/react/message-template)

## Pagination, Batching, and Performance

Pagination configuration is component-specific. See individual component pages such as [Conversations](/ui-kit/react/conversations), [Users](/ui-kit/react/users), [Groups](/ui-kit/react/groups), [Group Members](/ui-kit/react/group-members), [Call Logs](/ui-kit/react/call-logs), or [Message List](/ui-kit/react/message-list) for pagination details including `setLimit` and infinite scroll behavior.

## Error Handling and Retries

Error handling is component-specific. Each component that supports error handling exposes an `onError` callback prop. See individual component pages such as [Conversations](/ui-kit/react/conversations), [Users](/ui-kit/react/users), or [Incoming Call](/ui-kit/react/incoming-call) for `onError`, `hideError`, and `errorView` prop documentation.

## Configuration and Environment

All components require `CometChatUIKit.init()` to be called with a valid `appId` and `region` before rendering, and the user must be logged in via `CometChatUIKit.login()`. Components are imported from the `@cometchat/chat-uikit-react` package. Call-related components additionally require the `@cometchat/calls-sdk-javascript` package. See [Methods](/ui-kit/react/methods) for init, login, and logout details, and [React.js Integration](/ui-kit/react/react-js-integration) for setup instructions.

## Files and Media

Files and media handling is component-specific. See the [Message Composer](/ui-kit/react/message-composer) for attachment and file upload support, and the [Message List](/ui-kit/react/message-list) for media message rendering. The [Message Template](/ui-kit/react/message-template) page documents custom media template rendering.

## Logging, Tracing, and Observability

To troubleshoot issues with any component, enable CometChat SDK debug logging by setting the log level to `DEBUG` during `CometChat.init()`. This outputs detailed SDK activity to the browser console. Individual component pages document component-specific events that can be used for observability. Refer to the CometChat SDK documentation for the full logging configuration API.

## Versioning, Beta Features, and Breaking Changes

### Current Version

This documentation covers CometChat React UI Kit V6 (`@cometchat/chat-uikit-react@6.x`).

### Upgrading from V5

If you are migrating from V5, see the [Upgrading from V5](/ui-kit/react/upgrading-from-v5) guide for a complete list of breaking changes and migration steps. */}
